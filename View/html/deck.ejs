<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>

    <h1>deck</h1>

    <form action="/criarCard?id=<%= data['id'] %>" method="post">
        <input type="text" name="titulo">
        <input type="text" name="desc">
        <button>Enviar</button>
    </form>

    <a href="/cardIA?id=<%= data['id'] %>">Gerar cards IA</a>

    <table>

        <tr>
            <th>titulo</th>
            <th>descrição</th>

        </tr>
        
            <% data['lista'].forEach(element => { %>

                    
                    <tr id="<%= element[2] %>" style="border: 2px solid black;" onclick="redirect(this.id)">
                        <th> <h4> <%= element[0] %> </h4> </th>
                        <th> <h4> <%= element[1] %> </h4> </th>
                    </tr>
                               
                    <% }); %>
        
    </table>
</body>
</html>